# 玩家期望回饋設定

1. 建立一份新的 Google 試算表，名稱可設為「不朽神話玩家回饋」。
2. 從試算表網址複製 `/d/` 與 `/edit` 中間的試算表 ID。
3. 在試算表選擇「擴充功能」→「Apps Script」。
4. 將專案內 `google-apps-script.gs` 的內容完整貼入編輯器。
5. 把程式最上方的 `請貼上試算表ID` 換成步驟 2 的 ID。
6. 按右上角「部署」→「新增部署作業」→ 類型選擇「網頁應用程式」。
7. 執行身分選擇「我」，存取權選擇「任何人」。
8. 完成授權後，複製結尾為 `/exec` 的網頁應用程式網址。
9. 打開網站的 `app.js`，把 `EXPECTATION_FEEDBACK_ENDPOINT` 的空字串換成該 `/exec` 網址。
10. 儲存、提交並推送網站檔案。

試算表將記錄：提交編號、匿名玩家編號、玩家期望、提交時間、來源與章節。不會要求玩家輸入姓名或電子郵件。
