# 玩家期望回饋設定

1. 建立一份新的 Google 試算表，名稱可設為「不朽神話玩家回饋」。
2. 在這份試算表上方選擇「擴充功能」→「Apps Script」。
3. 刪除編輯器原本的程式，將專案內 `google-apps-script.gs` 的內容完整貼入。
4. 按上方的儲存圖示。
5. 按右上角「部署」→「新增部署作業」。
6. 類型選擇「網頁應用程式」。
7. 執行身分選擇「我」，存取權選擇「任何人」。
8. 完成授權後，複製結尾為 `/exec` 的網頁應用程式網址。
9. 將這個 `/exec` 網址交給 Codex 接入網站。

試算表將記錄：提交編號、匿名玩家編號、玩家期望、提交時間、來源與章節。不會要求玩家輸入姓名或電子郵件。
